# 🚀 Quick Reference: Chat System

## Быстрая справка для разработчиков

---

## 📞 AJAX Endpoints

### 1. Отправить сообщение
```javascript
jQuery.ajax({
    url: ajaxurl,
    type: 'POST',
    data: {
        action: 'ar_send_chat_message',
        order_id: 123,
        receiver_id: 456,
        message: 'Привет!',
        nonce: ar_chat_nonce
    }
});
```

**Ограничения:**
- ⏱️ Rate limit: 1 сообщение / 2 секунды
- 📏 Max длина: 1000 символов
- 🔒 Требуется авторизация

**Возможные ошибки:**
- `Слишком частая отправка сообщений` - подождите 2 сек
- `Доступ к чату запрещен` - нет прав
- `Сообщение слишком длинное` - > 1000 символов

---

### 2. Получить сообщения
```javascript
jQuery.ajax({
    url: ajaxurl,
    type: 'POST',
    data: {
        action: 'ar_get_chat_messages',
        order_id: 123,
        other_user_id: 456,
        nonce: ar_chat_nonce
    }
});
```

**Возвращает:**
```javascript
{
    success: true,
    data: {
        messages: [
            {
                id: 1,
                sender_id: 123,
                receiver_id: 456,
                message: "Текст сообщения",
                timestamp: "2025-12-24 12:00:00",
                formatted_time: "12:00",
                is_sent: true,
                is_read: true
            }
        ],
        other_user: {
            name: "Иван Иванов",
            is_online: true
        },
        current_user_id: 123
    }
}
```

---

### 3. Проверить новые сообщения
```javascript
jQuery.ajax({
    url: ajaxurl,
    type: 'POST',
    data: {
        action: 'ar_check_new_messages',
        nonce: ar_chat_nonce
    }
});
```

**Возвращает:**
```javascript
{
    success: true,
    data: {
        unread_count: 5
    }
}
```

**Периодичность:** Каждые 10 секунд

---

## 🔐 Права доступа

### Кто может видеть чат?

✅ **Разрешено:**
- Клиент, создавший заявку
- Дизайнер, выбранный для заявки

❌ **Запрещено:**
- Другие клиенты
- Другие дизайнеры
- Незарегистрированные пользователи

### Статусы заказов с запретом чата:
- `deleted` - удален
- `cancelled` - отменен
- `rejected` - отклонен

---

## 📊 База данных

### Таблица: `wp_designer_chat_messages`

```sql
CREATE TABLE wp_designer_chat_messages (
    id bigint(20) AUTO_INCREMENT PRIMARY KEY,
    order_id bigint(20) NOT NULL,
    sender_id bigint(20) NOT NULL,
    receiver_id bigint(20) NOT NULL,
    message text NOT NULL,
    attachment varchar(255) DEFAULT NULL,
    is_read tinyint(1) DEFAULT 0,
    created_at datetime DEFAULT CURRENT_TIMESTAMP,
    KEY order_id (order_id),
    KEY sender_id (sender_id),
    KEY receiver_id (receiver_id),
    KEY is_read (is_read)
);
```

---

## 🔍 Отладка

### Включить WordPress Debug Log

В `wp-config.php`:
```php
define('WP_DEBUG', true);
define('WP_DEBUG_LOG', true);
define('WP_DEBUG_DISPLAY', false);
```

### Поиск логов чата
```bash
tail -f wp-content/debug.log | grep AR_CHAT
```

### Примеры логов

✅ **Успешная отправка:**
```
(нет логов - все ок)
```

❌ **Ошибка доступа:**
```
AR_CHAT: Access denied. User: 123, Order: 456, Other: 789, Client: 789, Designer: 999
```

❌ **SQL ошибка:**
```
AR_CHAT: Failed to insert message. Error: Table 'wp_designer_chat_messages' doesn't exist
```

---

## ⚡ Performance Tips

### Оптимизация запросов

1. **Индексы созданы** ✅
   - `order_id`
   - `sender_id`
   - `receiver_id`
   - `is_read`

2. **Кеширование**
   ```php
   // Используется WordPress transient для rate limiting
   get_transient('ar_chat_last_message_' . $user_id);
   ```

3. **Пагинация сообщений** (TODO)
   ```php
   // Текущая реализация загружает ВСЕ сообщения
   // Рекомендуется добавить LIMIT
   ```

---

## 🐛 Частые проблемы

### 1. "Таблица чата не найдена"
**Решение:** Запустить `ar_create_chat_table()`
```php
ar_check_chat_table(); // Автоматически создаст таблицу
```

### 2. "Доступ к чату запрещен"
**Проверить:**
- Пользователь авторизован?
- Дизайнер выбран для заказа?
- Статус заказа не `deleted/cancelled/rejected`?

### 3. Rate limiting не работает
**Причины:**
- Object cache overrides transients
- Persistent object cache enabled

**Решение:** Использовать options вместо transients

### 4. Сообщения не помечаются прочитанными
**Проверить:**
- SQL запрос `UPDATE` выполняется?
- Есть права на UPDATE?
- Логи показывают ошибки?

---

## 🧪 Тесты

### Ручной тест #1: Отправка сообщения
```
1. Войти как клиент
2. Открыть заявку с выбранным дизайнером
3. Нажать "Чат с дизайнером"
4. Написать "Привет!"
5. Нажать Отправить
6. Сообщение должно появиться справа (синее)
```

### Ручной тест #2: Rate Limiting
```
1. Открыть консоль браузера
2. Выполнить 3 раза подряд:
   arSendChatMessage(event)
3. Должна появиться ошибка после 2-го сообщения
```

### Ручной тест #3: Права доступа
```
1. Войти как Дизайнер A
2. Скопировать URL чата Дизайнера B
3. Вставить в браузер
4. Должно показать "Доступ к чату запрещен"
```

---

## 📞 Контакты

**При проблемах:**
1. Проверить логи: `wp-content/debug.log`
2. Искать: `AR_CHAT:`
3. Создать issue с логами

**Документация:**
- `CHAT_SYSTEM_FIXES.md` - полная документация
- `КРАТКОЕ_РЕЗЮМЕ.md` - быстрое резюме
- `verify_chat_fixes.sh` - верификация

---

**Версия:** 1.0  
**Обновлено:** 24.12.2025
